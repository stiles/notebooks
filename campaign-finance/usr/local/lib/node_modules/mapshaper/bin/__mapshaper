#!/usr/bin/env node
var mapshaper = require("../mapshaper.js");

var args = process.argv.slice(2).join(' ');
mapshaper.runCommands(args, {xl: true}, function(err) {
  if (err) {
    mapshaper.printError(err);
    process.exit(1);
  } else {
    process.exit(0);
  }
});
